<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!--
    namespace는 CodeMapper 인터페이스의 전체 경로와 정확히 일치해야 합니다.
-->
<mapper namespace="kr.or.ddit.common.code.mapper.CodeMapper">

    <!--
        [공통 코드 그룹 목록 조회]
        - CODE_GROUP 테이블의 모든 데이터를 조회합니다.
    -->
    <select id="getCodeGroupList" resultType="kr.or.ddit.common.code.vo.CodeGroupVO">
        SELECT
            CODE_GROUP_ID,
            CODE_GROUP_NAME
        FROM
            CODE_GROUP
        ORDER BY
            CODE_GROUP_ID
    </select>

    <!--
        [공통 상세 코드 목록 조회]
        - 특정 그룹 ID에 해당하는 상세 코드 목록을 조회합니다.
        - USE_YN이 'Y'인 코드만 가져와서, 화면에는 사용 가능한 코드만 보이도록 합니다.
    -->
    <select id="getCodeDetailList" parameterType="String" resultType="kr.or.ddit.common.code.vo.CodeDetailVO">
        SELECT
            CODE_ID,
            CODE_GROUP_ID,
            CODE_NAME,
            USE_YN
        FROM
            CODE_DETAIL
        WHERE
            CODE_GROUP_ID = #{codeGroupId}
            AND USE_YN = 'Y'
        ORDER BY
            CODE_ID
    </select>
    
    <select id="getDistrictList" resultType="kr.or.ddit.common.vo.code.CodeDistrictVO">
        SELECT DISTRICT_ID, DISTRICT_NAME
        FROM CODE_DISTRICT
        ORDER BY DISTRICT_ID
    </select>
	
	
	
</mapper>
